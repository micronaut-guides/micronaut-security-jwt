micronaut:
  security:
    enabled: true # <1>
    endpoints:
      login:
        enabled: true # <2>
      oauth:
        enabled: true # <3>
    token:
      jwt:
        enabled: true # <4>
        signatures:
          secret:
            generator: # <5>
              secret: "${JWT_GENERATOR_SIGNATURE_SECRET:pleaseChangeThisSecretForANewOne}" # <6>
